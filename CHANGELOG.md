v1.10.0
-------
- Change the way `fetchData` works. It shouldn't return a promise anymore but an object (or array of object) specifying what to fetch and the name under which the data must be available.
